extends ../layout/index

append js
  != js("libs/Stats")
  != js("canvas/"+canvasScript+"/index")

append css
  != css("canvas")
  :stylus
    #main
      padding 0
      #content-wrapper
        margin 0

block content
  #content
    #canvas-container.unselectable
      canvas#canvas(oncontextmenu="return false;")
        h4 :(
        h4 Your browser doesn't support canvas.
      #canvas-controls-container
        h4#toggle-menu
          a Show
          |  controls.
        #canvas-controls
